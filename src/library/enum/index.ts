export enum language {
  Pt = "pt-BR"
}

export enum methods {
  POST = "post",
  PUT = "put",
  DELETE = "delete",
  GET = "get",
  PATCH = "patch"
}

export enum SentryLevelEnum {
  DEBUG = "debug",
  INFO = "info",
  WARNING = "warning",
  ERROR = "error",
  FATAL = "fatal"
}

export enum ContextDialogflow {
  user = "user-information"
}
